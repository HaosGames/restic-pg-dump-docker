backups:
  - name: example
    namespace: default
    dbAppConfig: cluster-example-app
    resticSecretName: restic-secret
    schedule: "*/5 * * * *"

extraVolumes:
  - name: restic-test-repo
    persistentVolumeClaim:
      claimName: restic-test-repo

extraVolumeMounts:
  - name: restic-test-repo
    mountPath: "/restic-test-repo"
